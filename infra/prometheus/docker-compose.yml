services:
  prometheus:
    image: prom/prometheus:v3.8.0
    container_name: personalization-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
